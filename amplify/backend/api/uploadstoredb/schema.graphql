type S3Object {
  bucket: String!
  region: String!
  identityID: String!
  key: String!
}

type Picture @model @auth(rules:[{allow:owner},{allow: groups, groupsField: "tenant", operations: [read]}]) {
  id: ID!
  name: String
  owner: String
  tenant: String
  file: S3Object
}